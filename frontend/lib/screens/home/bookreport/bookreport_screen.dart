import 'package:flutter/material.dart';

class BookReportScreen extends StatelessWidget {
  const BookReportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        '추천 들어갈 페이지',
        style: TextStyle(
          fontSize: 50,
        ),
      ),
    );
  }
}
